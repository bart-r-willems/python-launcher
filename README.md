# python-launcher
Tkinter based GUI to launch interactive Python apps in directory and venv of choice. The GUI shows a list of availailable environments and folders, as well as a prefefined list of executables than can be called from within the environment.
